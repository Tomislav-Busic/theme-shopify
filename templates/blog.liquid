<h1>{{ blog.title }}</h1>

{% for article in blog.articles %}
  <div>
    <a href="{{ article.url }}">
      <h2>{{ article.title }}</h2>
      <p>{{ article.created_at }}</p>
      <p>{{ article.author }}</p>
      <div>{{ article.excerpt }}</div>
    </a>
  </div>
{% endfor %}